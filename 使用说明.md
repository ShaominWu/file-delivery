# Ferrum File Delivery - Windows 使用说明

## 文件夹结构

把整个 file-delivery 文件夹放到 C:\file-delivery，结构如下：

```
C:\file-delivery\
├── config\
│   ├── clients.json          ← 客户配置（已设好）
│   ├── dropbox_refresh_token.txt  ← Dropbox凭证（如有）
│   └── onedrive_credentials.json  ← OneDrive凭证（如要清理OneDrive）
├── tools\
│   ├── delivery_agent.py     ← 主程序
│   ├── cleanup_drive.py      ← 清理工具
│   ├── dropbox_uploader.py   ← Dropbox模块
│   ├── first_auth.py         ← 首次授权工具
│   └── client_secrets.json   ← ⚠️ 需要你放进去（Google Cloud下载）
├── logs\                     ← 自动生成，存日志
├── run_delivery.bat          ← 发送文件（可手动双击测试）
├── run_cleanup.bat           ← 清理云端（可手动双击测试）
├── first_auth.bat            ← 第一次授权Google Drive
├── install_python_libs.bat   ← 安装Python库
└── install_scheduler.ps1     ← 安装定时任务
```

---

## 第一次安装步骤

### 第1步：确认Python已安装
打开命令行（Win+R，输入cmd），输入：
```
python --version
```
如果显示版本号就OK。如果没有，去 python.org 下载安装。

### 第2步：安装Python依赖库
双击运行 `install_python_libs.bat`

### 第3步：放入 Google Drive 凭证
1. 去 https://console.cloud.google.com
2. 下载 OAuth 2.0 凭证文件
3. 重命名为 `client_secrets.json`
4. 放到 `tools\` 文件夹里

### 第4步：首次授权 Google Drive（只做一次）
双击运行 `first_auth.bat`，浏览器会弹出，点允许即可。

### 第5步：修改同步文件夹路径
先把 `config\clients.example.json` 复制为 `config\clients.json`。
再用记事本打开 `config\clients.json`，找到这行：
```
"windows_sync_folder": "C:\\绿联云同步"
```
改成你绿联云同步的实际路径（注意：反斜杠要写两个 `\\`）。

### 第6步：安装定时任务
1. 用记事本打开 `install_scheduler.ps1`
2. 修改第8行的 `C:\file-delivery` 为你实际放的路径
3. 右键 → 用PowerShell运行（或以管理员身份运行）

---

## 日常使用

**完全不需要你操作**，电脑开着就会自动：
- 每天 08:00 扫描并发送
- 每天 10:00 再次扫描并发送
- 每天 11:30 再次扫描并发送
- 每天 01:00 清理7天以上的 Google Drive / OneDrive 云端文件

OneDrive 清理需要先把 `config/onedrive_credentials.example.json` 复制为
`config/onedrive_credentials.json`，并填入 Microsoft 应用的 `client_id` 和
`refresh_token`。如果没有这个文件，程序会继续清理 Google Drive，
并在日志里提示 OneDrive 未配置。

Dropbox 清理已经支持，但默认定时任务不会自动删除 Dropbox 文件。需要时可手动运行：
`python3 tools/cleanup_drive.py --days 7 --cloud dropbox`

**手动测试**：双击 `run_delivery.bat` 可以立刻运行一次。

**查看日志**：打开 `logs\` 文件夹，每次运行都有一个日志文件。

---

## 如果电脑关机了怎么办？

定时任务设置了 **"StartWhenAvailable"**，意思是：如果错过了预定时间（比如电脑关着），
下次开机后会自动补跑。所以不用担心。

---

## 常见问题

**问：换电脑了怎么办？**
把整个文件夹复制过去，重新运行 `first_auth.bat` 授权一次，再运行 `install_scheduler.ps1`。

**问：token.pickle 是什么？**
Google Drive 的登录凭证，自动生成，不要删除。

**问：在哪里查看定时任务？**
搜索「任务计划程序」→ 任务计划程序库 → 找 Ferrum_Delivery_0800 等。
